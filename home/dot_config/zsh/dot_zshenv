#  ~/.config/zsh/.zshenv
# Core envionmental variables
# Locations configured here are requred for all other files to be correctly imported

# XDG Base Directory Specification (https://specifications.freedesktop.org/basedir-spec/basedir-spec-latest.html)
export XDG_CONFIG_HOME="${HOME}/.config"
export XDG_DATA_HOME="${HOME}/.local/share"
export XDG_BIN_HOME="${HOME}/.local/bin"
export XDG_LIB_HOME="${HOME}/.local/lib"
export XDG_CACHE_HOME="${HOME}/.cache"
export XDG_STATE_HOME="$HOME/.local/state"
export XDG_RUNTIME_DIR="$HOME/.local/runtime" # macOS does not have session lifetime directories; alt: `~/Library/Caches`

#export PATH="~/.local/bin:$PATH"
export PATH="$HOME/.local/bin:$PATH"

# Set default applications
export EDITOR="nvim"
export VISUAL="code"
export TERMINAL="ghostty"
# export BROWSER="zen-browser"
#export PAGER="less"

# Respect XDG directories
export ZDOTDIR="${XDG_CONFIG_HOME}/zsh"
export ZELLIJ_CONFIG_DIR="${XDG_CONFIG_HOME}/zellij"

# Encodings and locale
export LANG=en_GB.UTF-8
export PYTHONIOENCODING='UTF-8'
# Note: LC_ALL deliberately not set - it overrides everything including user prefs
# Set individual LC_* only if you need to override specific categories


# Hide computer name in terminal
export DEFAULT_USER="$(whoami)"

# Note: .env is loaded in .zshrc with proper comment handling

# Note: pandoc alias moved to aliases.zsh
# Note: history settings moved to .zshrc (interactive shell only)

# Beads related: Daemon mode does NOT work correctly with `git worktree`.
# Git worktrees share the same `.git` directory and `.beads` database.
# Uncomment if needed:
# export BEADS_NO_DAEMON=1
